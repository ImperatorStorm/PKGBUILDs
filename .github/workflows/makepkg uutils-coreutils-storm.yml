name: Makepkg CI - uutils-coreutils-storm

env:
  GPG_TTY: $(tty)

on:
  push:
    paths:
        - "uutils-coreutils-storm/**"
        - "makepkg.conf"
        - ".github/workflows/makepkg uutils-coreutils-storm.yml"
  workflow_dispatch:

jobs:
  pkgbuild:
    runs-on: ubuntu-latest
    container: archlinux/archlinux:base-devel
    steps:
    - name: Checkout
      uses: actions/checkout@v2
    - name: Makepkg
      run: |
        pacman -Syu --noconfirm
        useradd builder -m
        echo "builder ALL=(ALL) NOPASSWD: ALL" >> /etc/sudoers
        chmod -R a+rw .
        BASEDIR="$PWD"
        cd uutils-coreutils-storm
        sudo -u builder echo -n "${{ secrets.GPG_PRIVATE_KEY }}" | sudo -u builder base64 --decode | sudo -u builder gpg --import --batch --no-tty
        sudo -u builder gpg --locate-keys torvalds@kernel.org gregkh@kernel.org
        sudo -u builder makepkg -s --sign --noconfirm --config=../makepkg.conf
    - name: Upload Packages
      uses: actions/upload-artifact@v2
      with:
          name: packages
          path: |
            uutils-coreutils-storm/*.zst
            uutils-coreutils-storm/*.zst.sig
            !uutils-coreutils-storm/*debug*.zst
            !uutils-coreutils-storm/*debug*.zst.sig
    #        ${{ steps.makepkg.outputs.pkgfile0 }}.sig
    - uses: actions/upload-artifact@v2
      with:
          name: debug_packages
          path: |
            uutils-coreutils-storm/*debug*.zst
            uutils-coreutils-storm/*debug*.zst.sig