name: Makepkg CI - Mono-MSbuild

env:
  GPG_TTY: $(tty)

on:
#  schedule:
#  - cron: '0 * * * *'
  push:
    paths:
        - "mono-msbuild/**"
        - "makepkg.conf"
        - ".github/workflows/makepkg mono-msbuild.yml"
  pull_request:
    paths:
        - "mono-msbuild/**"
        - "makepkg.conf"
        - ".github/workflows/makepkg mono-msbuild.yml"
  workflow_dispatch:

jobs:
  pkgbuild:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v2
    - name: Makepkg Build
      id: makepkg
      uses: edlanglois/pkgbuild-action@v1
      with:
          pkgdir: "mono-msbuild"
          makepkgArgs: "--skippgpcheck --config=../makepkg.conf"
    #- name: Import GPG key
    #  id: import_gpg
    #  uses: crazy-max/ghaction-import-gpg@v4
    #  with:
    #      gpg_private_key: ${{ secrets.GPG_PRIVATE_KEY }}
    #      passphrase: ${{ secrets.PASSPHRASE }}
    #- name: Sign Packages
    #  run: |
    #    gpg --sign --detach-sign ${{ steps.makepkg.outputs.pkgfile0 }}
    #    gpg --sign --detach-sign ${{ steps.makepkg.outputs.pkgfile1 }}
    - name: Upload Packages
      uses: actions/upload-artifact@v2
      with:
          path: |
            ${{ steps.makepkg.outputs.pkgfile0 }}
            ${{ steps.makepkg.outputs.pkgfile1 }}
            ${{ steps.makepkg.outputs.pkgfile2 }}
            ${{ steps.makepkg.outputs.pkgfile3 }}
    #        ${{ steps.makepkg.outputs.pkgfile0 }}.sig